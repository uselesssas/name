<h1>All posts</h1>

<table>
  <tr>
    <td>User name</td>
    <td>Post description</td>
    <td>Settings</td>
  </tr>
  <% @posts.each do |post| %>
    <tr>
      <td><%= post.user.first_name %></td>
      <td><%= post.description %></td>
      <td>
        <%= link_to 'Show', post_path(post) %>
        <%= link_to 'Edit', edit_post_path(post) if policy(post).edit? %>
        <%= link_to 'Delete', post_path(post), method: :delete if policy(post).destroy? %>
      </td>
    </tr>
  <% end %>
</table>
